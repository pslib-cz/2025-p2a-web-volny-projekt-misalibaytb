{
  "name": "projektskola",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "browser-sync": "^3.0.4",
    "prettier": "^3.8.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "script:build": "bun build ./compile.ts --minify --target=bun --outfile=./index.ts && bun -e \"const f='index.ts'; const s=await Bun.file(f).text(); await Bun.write(f, '// script made by misaliba\\n' + s.split('\\n').slice(1).join('\\n'))\""
  }
}
